{
  "tags": [
    "asset"
  ],
  "summary": "Fetch Asset File",
  "description": "Fetch asset file from storage by UID, asset type, and entity name.",
  "properties": {
    "uid": {
      "type": "uid"
    },
    "type": {
      "type": "string-not-empty",
      "description": "Type of asset.",
      "enum": [
        "avatar",
        "banner"
      ]
    },
    "entity": {
      "type": "string-not-empty",
      "description": "Entity name.",
      "enum": [
        "institution",
        "people"
      ]
    }
  },
  "required": [
    "uid",
    "type",
    "entity"
  ]
}
